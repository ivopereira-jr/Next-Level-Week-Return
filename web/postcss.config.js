/* 
  depois de instalar o tailwindcss vc deve inserir no terminal npx tailwindcss init -p então esse arquivo sera gerado ele se integra ao vite para ficar observando ao alterações nos arquivos de css e com isso fazer as compilações dos estilos 
*/

module.exports = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
}
